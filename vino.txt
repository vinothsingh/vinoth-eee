#include<stdio.h>

int main(void) {
	int a;
	
	if(a!=0)
	{
		if(a>0)
		printf("/n positive number");
		else
		printf("/n negative number");
	}
	else
	printf("number is zero");
	
	return (0);
}